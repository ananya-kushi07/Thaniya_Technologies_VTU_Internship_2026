# Day 12 – Express Middleware & Request Lifecycle

Today, I learned about Express middleware and its role in the request–response lifecycle of backend applications. Middleware functions execute before a request reaches the final route handler and can perform tasks such as logging, authentication, validation, and error handling.

I practiced creating a simple middleware function to log request details like HTTP method and URL before processing the request.